import type { CogitaConfig } from './types';

/**
 * A helper function to define the cogita config.
 * It provides type-safety and auto-completion.
 */
export function defineConfig(config: CogitaConfig): CogitaConfig {
  return config;
}
