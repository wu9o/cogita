# __SITE_TITLE__

这是一个由 Cogita `blog` 模板生成的静态博客。

```bash
pnpm install
pnpm run doctor
pnpm run dev
```

编辑 `posts/` 中的 Markdown 文件即可发布文章。构建产物位于 `doc_build/`。

升级依赖或提交部署前，可以运行 `pnpm run doctor -- --strict` 检查配置、依赖、主题和内容目录。
