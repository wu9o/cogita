# __SITE_TITLE__

This static blog was generated from the Cogita `blog` template.

```bash
pnpm install
pnpm run doctor
pnpm run dev
```

Edit Markdown files in `posts/` to publish articles. Build output is written to `doc_build/`.

Before upgrading dependencies or deploying, run `pnpm run doctor -- --strict` to check configuration, dependencies, theme, and content directories.
