# Cogita starters

这里提供可以复制到独立项目中的第三方扩展起始模板：

- `plugin`：基于 `CogitaPluginFactory` 的构建插件。
- `theme`：包含首页布局和全局样式的主题包。

starter 不属于 Cogita workspace，也不会被框架发布流程自动发布。复制后请先替换 `package.json` 中的占位包名，再根据业务补充配置、运行时模块和测试。
